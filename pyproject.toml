[project]
name = "axi-easyagent"
version = "0.3.1"
description = "A lightweight Python AI agent framework"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "httpx>=0.28.1",
    "httpx-sse>=0.4.3",
]

